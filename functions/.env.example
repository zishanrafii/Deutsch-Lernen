# functions/.env
# Non-secret config (secrets are set separately via `firebase functions:secrets:set`).
# Copy this file to functions/.env and fill in the real value, or create
# functions/.env directly — either way it must NOT be committed to a public repo.

SSLCOMMERZ_IS_LIVE=false
